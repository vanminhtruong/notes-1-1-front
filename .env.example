# Frontend environment variables example

# Preferred backend URLs (set these in production or when backend runs on a different host)
# If not set, the app will use the current page hostname and default to port 3000
VITE_BACKEND_HTTP_URL=http://localhost:3000
VITE_BACKEND_WS_URL=http://localhost:3000

# Third-party auth keys (placeholders)
VITE_GOOGLE_CLIENT_ID=
VITE_FACEBOOK_APP_ID=
